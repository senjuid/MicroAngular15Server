import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'MicroAngular15Server',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
